// Slice é a função que tira uma fatia do array. O primeiro parâmetro é onde a fatia começa, e o segundo parâmetro é uma posição a mais aonde você quer que a fatia termine.

const cores = ["azul", "amarelo", "vermelho", "verde"]

const pedaco = cores.slice(0,2)

console.log(pedaco);