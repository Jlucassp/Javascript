// Join é uma função que transforma o array em uma string, onde você pode ou não determinar um separador para os elementos do array.

const cores = ["azul", "amarelo", "vermelho", "verde"]

const texto = cores.join(" ")

console.log(cores);
console.log(texto);